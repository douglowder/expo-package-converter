{
  "name": "expo-project-modifier-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Monorepo for Expo project modifier packages",
  "scripts": {
    "postinstall": "expo-yarn-workspaces check-workspace-dependencies",
    "prepare": "lerna run prepare --sort"
  },
  "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "expo": ">=52.0.0"
  },
  "devDependencies": {
    "@expo/package-manager": "^1.5.2",
    "eslint": "^8.10.0",
    "expo-yarn-workspaces": "^2.3.2",
    "lerna": "^3.4.1",
    "prettier": "^3",
    "ts-node": "^10.0.0",
    "typescript": "^5.1.3"
  },
  "packageManager": "yarn@1.22.22"
}
